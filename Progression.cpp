#include <iostream>

using namespace std;
using ll = long long;

ll progression(ll b, ll a = 0) {
    return (b - a + 1) * (a + b) / 2;
    //    arithmetic progression
    //an = a1 + (n-1)d
    //Sn  = (a1 + an)*n/2
}

int main() {
//geometric progression
//    RES = rd-l/(d-1)
}